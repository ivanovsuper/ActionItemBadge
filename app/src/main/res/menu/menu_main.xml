<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.ivanovsuper.actionitembadge.MainActivity">
    <item
        android:id="@+id/badge"
        android:actionLayout="@layout/badge_counter_layout"
        android:icon="@drawable/badge_square_rounded"
        android:title="@string/app_name"
        app:showAsAction="always">
    </item>
    <item
        android:id="@+id/icon_badge"
        android:actionLayout="@layout/badge_counter_layout_circle"
        android:icon="@android:drawable/ic_menu_recent_history"
        android:title="@string/app_name"
        app:showAsAction="always">
    </item>
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never"/>
</menu>
